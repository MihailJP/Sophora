#!/usr/local/bin/mensis

if ($argc < 2)
	Print("Usage: "+$0+" font-file")
	Quit(1)
endif

Print("Reading source files...")
Open($1)

SetTableField("advanceWidthMax", GetTableOffset(4*3, 2, 'hmtx'), 'hhea')
Save()

Close()
